# Intentionally empty: analytics is disabled during `npm run dev` so local
# clicks don't pollute the production Google Analytics stream.
# Override per-developer in `.env.local` (gitignored) if you want to test GA.
VITE_GA_ID=
